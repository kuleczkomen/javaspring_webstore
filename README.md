# This is a backend written in Java for a webstore. <br> Tech stack: Spring Boot, Spring Security, Hibernate, PostgreSQL.
## The app handles users, products, and orders. <br> It's secured by password hashing (bcrypt) and jwt. <br> Also, some methods are for admin only. <br/> Data is stored in a PostgreSQL database.
